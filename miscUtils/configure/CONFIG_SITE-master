# If you don't want to install into $(TOP) then
# define INSTALL_LOCATION_APP here
INSTALL_LOCATION_APP=$(TOP)/../..
INSTALL_LOCATION=$(TOP)/../..
ifneq ($(EPICS_HOST_ARCH),solaris-sparc-gnu)
CROSS_COMPILER_TARGET_VME = RTEMS-svgm RTEMS-beatnik
else
CROSS_COMPILER_TARGET_VME =
endif

MSI=$(EPICS_BASE)/../extensions/bin/$(EPICS_HOST_ARCH)/msi

CONFIG_INSTALLS = ../RULES_BUILD
